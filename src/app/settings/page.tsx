import { Settings } from "lucide-react";
import { ComingSoon } from "@/components/ComingSoon";

// 設定。実装までの準備中プレースホルダ。
export default function SettingsPage() {
  return <ComingSoon title="設定" icon={Settings} active="settings" />;
}
